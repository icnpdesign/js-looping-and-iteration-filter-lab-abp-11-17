// Code your solution in this file
const list = ['Bobby', 'Sammy', 'Sally', 'Annette', 'Sarah', 'Bobby'];

function findMatching(list, name) {
  return list.filter(function (drivername) {
    return driverName.toLowercase() === name.toLowerCase();
  });
}
